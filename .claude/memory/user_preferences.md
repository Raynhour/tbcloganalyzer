---
name: User tech preferences
description: User prefers Vue 3 + NestJS + GraphQL stack with full test coverage
type: user
---

- Prefers Vue 3 with Composition API (`<script setup lang="ts">`)
- Backend: NestJS with TypeScript
- Wants GraphQL end-to-end (not REST) — Apollo Client on FE, @nestjs/graphql on BE
- Wants unit testable code
- Uses Tailwind CSS for styling
